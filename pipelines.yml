pipelines:
  - name: basic2
    steps:
      - name: basic2
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - printenv
